# الأمان

## نطاق
هذه السياسة تنظّم الإبلاغ عن الثغرات في جميع المكونات: API، تطبيقات Flutter، لوحات Next.js.

## الإبلاغ
نرحب بإبلاغات الباحثين الأمنيين. أرسل تقريرك إلى security@example.com مع:
1. وصف الثغرة.
2. خطوات إعادة الإنتاج.
3. سيناريو الاستغلال المحتمل.
4. اقتراح للحلّ إن وُجد.

## الإفصاح
نلتزم بفترة إفصاح منسّق مدّتها 90 يوماً من تاريخ التأكد من الثغرة قبل النشر العام.

## الممارسات الأمنية
- TLS إلزامي على جميع الـ endpoints.
- JWT Access (15 د) + Refresh مع كشف إعادة الاستخدام.
- Argon2id لتجزئة جميع الأسرار (OTP، Refresh Tokens، Passwords).
- Rate Limiting و Throttling من جانب الـ Backend.
- Audit log لكل عملية حساسة.
- مكان مخصص للـ 2FA (TOTP) في نموذج المستخدم.
- لا نخزن بيانات بطاقات بنكية - نعتمد على tokenization من مزودي الدفع.
- جميع secrets في متغيرات بيئة، ولا تُلتزم في الـ git.
